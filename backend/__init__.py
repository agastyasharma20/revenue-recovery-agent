"""FastAPI backend over core/. A thin HTTP/WebSocket layer -- all decision
logic still lives in core/, exactly as it does for run_evaluation.py and
the other CLI demo scripts. This package doesn't reimplement anything."""
